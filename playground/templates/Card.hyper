title: str = "Untitled"
color: str = "blue"

print(color)

for i in range(3):
    <span>Test {'test'.__str__}</span>

<div class="card card-{color}">
  <h3>{title}</h3>
  <div class="card-body">{...}</div>
</div>


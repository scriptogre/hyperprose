# LEXER TEST: Less-than comparisons vs HTML
# These lines should be PYTHON_LINE, not HTML_LINE
a: int = 10
b: int = 5

# Less-than comparison (NOT HTML!)
result = a < b
result2 = 5 < 10
x = a<b
is_valid = len([1,2,3]) < 100

# Greater-than (definitely not HTML)
result3 = a > b

# Both in one expression
in_range = a < 50 and a > 0

# Chained comparison (valid Python)
chained = 0 < a < 100

<span>Comparison result: {result}</span>

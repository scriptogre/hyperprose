# LEXER TEST: The 'end' keyword edge cases
value: int = 0

# This IS an end keyword (closes the if block)
if True:
    <span>Content</span>
end

# These are NOT end keywords (followed by more text)
endpoint = "/api"
end_marker = "---"
endswith = "hello".endswith("o")

# end with whitespace variations
if True:
    <span>A</span>
end
if True:
    <span>B</span>
  end
if True:
    <span>C</span>
	end

# Multiple ends in sequence (for nested blocks)
if True:
    if True:
        if True:
            <span>Deep</span>
        end
    end
end

<span>endpoint = {endpoint}</span>

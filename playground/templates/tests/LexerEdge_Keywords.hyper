# LEXER TEST: Variables that contain keywords
# All these should be PYTHON_LINE, not CONTROL_LINE
dummy: int = 0

# 'if' in identifier
if_enabled = True
notify_if_ready = False

# 'for' in identifier
format_string = "hello"
formula = 42
information = {}

# 'while' in identifier
meanwhile = "processing"
awhile = True

# 'match' in identifier
match_result = None
rematch = False

# 'def' in identifier
default = 0
undefined = None
definition = "term"

# 'class' in identifier
class_name = "button"
classify = lambda x: x

# 'end' in identifier (critical!)
endpoint = "/api/users"
end_date = "2024-01-01"
backend = "postgres"
frontend = "react"

# 'case' in identifier
case_study = "example"
lowercase = "abc"

# 'try' in identifier
retry = 3
try_again = True

# 'else' in identifier
elsewhere = "location"
something_else = True

<span>All variables assigned without triggering control flow</span>
